# API服务器配置示例
# 复制此文件为 .env.local 并根据实际情况修改

# API服务器主机地址（可选）
# 如果不设置，将自动使用当前访问的主机IP
# REACT_APP_API_HOST=10.255.11.3

# API服务器端口（默认8000）
REACT_APP_API_PORT=8000

# 开发环境下的备用IP地址列表（用逗号分隔）
# REACT_APP_DEV_IPS=10.255.11.3,192.168.1.100,192.168.0.100

# 是否启用IP自动检测（默认true）
# REACT_APP_AUTO_DETECT_IP=true

# 网络超时设置（毫秒）
# REACT_APP_NETWORK_TIMEOUT=5000
